{
  "name": "system-design-learner",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "node --test scripts/readme-smoke-e.test.mjs",
    "lint": "turbo run lint --filter=!system-design-learner",
    "db:generate": "pnpm --filter @sdl/api db:generate",
    "db:migrate": "pnpm --filter @sdl/api db:migrate",
    "db:push": "pnpm --filter @sdl/api db:push"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
