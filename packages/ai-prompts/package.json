{
  "name": "@sdl/ai-prompts",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"@sdl/ai-prompts: no tests yet\" && exit 0"
  },
  "dependencies": {
    "@sdl/shared": "workspace:*"
  }
}
